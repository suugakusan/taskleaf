ja:
  activerecord:
    errors:
      messages:
        record_invalid: "バリデーションに失敗しました: %{error}"
        restrict_dependent_destory:
          has_one: "%{record}が存在していろので削除できません"
          has_many: "%{record}が存在していろので削除できません"
    models:
      task: タスク
    attributes:
      task:
        id: ID
        name: 名称
        description: 詳しい説明
        created_at: 登録日時
        updated_at: 更新日時